<a href="/post/{{id}}">
  <div class="card">

    <h3 class="card-header">{{title}} </h3>
    <h4 class="card-header">Posted by {{user.username}} today</h4>

    <div class="container">

      <p class="post-text">{{post_content}}</p>
    </div>
  </div>

</a>